version: "3.9"

services:
  noc-monitor:
    image: noc-monitor:latest
    container_name: noc-monitor
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./config.json:/app/config.json:ro
      - ./logs:/app/logs
    restart: unless-stopped